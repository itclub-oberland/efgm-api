let {should, expect} = require("../configuration");

describe("User Model Test Suite", () => {
    describe("User Model", () => {
        it("should not be empty on initialization", () => {

        })
    })
});